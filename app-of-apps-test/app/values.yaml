appName: shlomo-test
namespace: default

image:
  name: devopsjourney1/mywebapp
  tag: latest
containerPort: 80
type: ClusterIP
configmap:
  name: default
  data:
    CUSTOM_HEADER: "This app was deployed using with helm"

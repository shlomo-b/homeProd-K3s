{{- range $name, $config := .Values }}
{{- if and $config.serviceAccount $config.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $config.cronjob_name }}
  namespace: cronjob
  labels:
    app: {{ $config.cronjob_name }}
{{- with $config.serviceAccount.annotations }}
  annotations:
{{ toYaml . | nindent 4 }}
{{- end }}
---
{{- end }}
{{- end }}
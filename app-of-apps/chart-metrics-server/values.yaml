metrics-server:
  serviceAccount:
    create: true
  replicas: 4
  resources:
    requests:
      cpu: 40m
      memory: 25Mi
    limits:
      cpu: 40m
      memory: 25Mi
  service:
    type: ClusterIP
    ports:
      https: 443
  apiService: # enable the api service for the metrics-server
    create: true
  args:
    - --kubelet-insecure-tls  
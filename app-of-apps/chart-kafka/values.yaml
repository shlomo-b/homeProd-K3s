kraft:
  enabled: true

controller:
  replicaCount: 3
  persistence:
    enabled: true
    storageClass: "local-path"
    size: 8Gi

listeners:
  client:
    protocol: PLAINTEXT
  controller:
    protocol: PLAINTEXT
  interbroker:
    protocol: PLAINTEXT

clusterId: ""
kraftVersion: 1